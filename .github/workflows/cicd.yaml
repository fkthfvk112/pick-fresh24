name: Build Image

on:
  push:
    branches:
      - "main"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build Container Image
        uses: docker/build-push-action@v4
        with:
          context: ./emart-fresh-spring/demo
          file: ./emart-fresh-spring/demo/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/back-end:latest

      - name: SSH Remote Commands
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: 22
          timeout: 40s
          script: |
            docker login -u ${{ secrets.DOCKERHUB_USERNAME }} -p ${{ secrets.DOCKERHUB_TOKEN }}
            docker pull ${{ secrets.DOCKERHUB_USERNAME }}/back-end:latest
            docker rm -f fresh-backend
            docker run -d \
              -p 3000:3000 \
              -e DB_HOST=mydb \
              -e DB_USER=${{ secrets.DB_USER }} \
              -e DB_PASSWORD=${{ secrets.DB_PASSWORD }} \
              -e EMAIL_USER=${{ secrets.NCS_ACCESSKEY }} \
              -e EMAIL_PASSWORD=${{ secrets.NCS_SECRETKEY }} \
              -e BOOTPAY_ID=${{ secrets.NCS_SERVICEID }} \
              -e BOOTPAY_PRIVATE_KEY=${{ secrets.NCS_PHONE_NUMBER }} \
              -e JWT_TOKEN=${{ secrets.JWT_TOKEN }} \
              -e NC_CLOUDNAME=${{ secrets.NC_CLOUDNAME }} \
              -e NC_API_KEY=${{ secrets.NC_API_KEY }} \
              -e NC_API_SECRET=${{ secrets.NC_API_SECRET }} \
              --name fresh-backend \
              --link mydb:db \
              ${{ secrets.DOCKERHUB_USERNAME }}/back-end:latest
